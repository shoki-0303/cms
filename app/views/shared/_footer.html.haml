%footer.footer
  .footer__categories
    %h3 CATEGORY
    %ul
      - @categories.each do |category|
        %li.footer_category
          = link_to category.name, category_path(category), class: "add_color"
  .footer__links
    %h3 ABOUT
    %ul
      %li.footer_link
        = link_to 'トップページ', root_path
      %li.footer_link
        = link_to 'カテゴリーページ', categories_path
  .footer__copyright
    %p copyright
